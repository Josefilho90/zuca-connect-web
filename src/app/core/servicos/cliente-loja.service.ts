import { Injectable } from '@angular/core';import { ClienteLoja } from '../modelos/loja.model';import { RepositorioLocal } from './repositorio-local.service';
@Injectable({providedIn:'root'})export class ClienteLojaService extends RepositorioLocal<ClienteLoja>{protected readonly chave='zuca_clientes_loja';obterClientes():ClienteLoja[]{return this.obterTodos();}obterClientePorId(id:string):ClienteLoja|undefined{return this.obterPorId(id);}criarCliente(cliente:Omit<ClienteLoja,'id'|'dataCadastro'>):ClienteLoja{return this.criar({...cliente,id:this.gerarId('loja'),dataCadastro:new Date().toISOString().slice(0,10)});}alterarCliente(cliente:ClienteLoja):ClienteLoja{return this.alterar(cliente);}}
