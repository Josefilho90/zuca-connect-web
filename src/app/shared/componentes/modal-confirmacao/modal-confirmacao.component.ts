import { ChangeDetectionStrategy, Component, input, output } from '@angular/core';
@Component({selector:'app-modal-confirmacao',template:'@if(aberto()){<div class="fundo" (click)="cancelar.emit()"><section (click)="$event.stopPropagation()"><i>!</i><h2>{{ titulo() }}</h2><p>{{ mensagem() }}</p><footer><button class="botao secundario" (click)="cancelar.emit()">Voltar</button><button class="botao perigo" (click)="confirmar.emit()">Confirmar</button></footer></section></div>}',styles:['.fundo{position:fixed;inset:0;background:#07120fcc;display:grid;place-items:center;z-index:100;padding:1rem}section{background:var(--superficie);border-radius:20px;padding:1.7rem;width:min(420px,100%);text-align:center}i{display:grid;place-items:center;width:48px;height:48px;background:#fff1df;color:#b46b19;border-radius:50%;margin:auto;font-style:normal;font-weight:700}h2{margin:.8rem 0 .3rem}p{color:var(--texto-suave)}footer{display:flex;justify-content:center;gap:.7rem;margin-top:1.3rem}'],changeDetection:ChangeDetectionStrategy.OnPush})
export class ModalConfirmacaoComponent{readonly aberto=input(false);readonly titulo=input('Confirmar ação');readonly mensagem=input('Deseja continuar?');readonly confirmar=output<void>();readonly cancelar=output<void>();}
