import { Component,inject,signal } from '@angular/core';import { DatePipe } from '@angular/common';import { PedidoLojaService } from '../../core/servicos/pedido-loja.service';import { CabecalhoPaginaComponent } from '../../shared/componentes/cabecalho-pagina/cabecalho-pagina.component';
@Component({selector:'app-meus-pedidos',imports:[DatePipe,CabecalhoPaginaComponent],templateUrl:'./meus-pedidos.component.html',styleUrl:'./loja.component.scss'})export class MeusPedidosComponent{private readonly service=inject(PedidoLojaService);readonly pedidos=signal(this.service.obterPedidosLoja());filtrar(status:string):void{this.pedidos.set(this.service.obterPedidosLoja().filter(p=>!status||p.status===status));}}
